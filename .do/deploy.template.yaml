spec:
  name: midsphere-www
  static_sites:
    - name: www
      git:
        branch: master
        repo_clone_url: https://github.com/Midsphere-AI/www.git
      build_command: npm install && npm run build
      output_dir: dist
      environment_slug: node-js
      routes:
        - path: /
